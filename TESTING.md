# Check-In — 테스트 시나리오

기능 구현 후 반드시 이 체크리스트대로 테스트한다.
모든 항목이 ✅ 되어야 배포 가능하다.

---

## 1단계 — 환경 확인

```
□ npm run dev 실행 후 오류 없음
□ localhost:3000 브라우저 접속 정상
□ Supabase 연결 확인 (로그인 시도)
□ Gemini API 호출 확인 (/api/ai/chat 테스트)
□ Claude API 폴백 확인 (Gemini 키 임시 제거 후 테스트)
```

---

## 2단계 — 인증 흐름

```
□ 회원가입: 이메일+비밀번호 입력 → 가입 완료
□ 로그인: 가입한 계정으로 로그인 → /projects 이동
□ 비로그인 상태에서 /dashboard 접근 → /login 리다이렉트
□ 로그인 상태에서 /login 접근 → /projects 리다이렉트
□ 로그아웃 → /login 이동
□ 새로고침 후 로그인 유지 확인
```

---

## 3단계 — 핵심 흐름 (업체)

### 3-1. 프로젝트 생성
```
□ /projects 에서 + 새 현장 버튼 클릭
□ 현장명, 고객명, 업종, 시작일 입력
□ 생성 완료 → /projects/[id] 이동
□ 생성된 프로젝트가 목록에 표시
□ 생성 시 DEFAULT_PROCESSES (철거/전기/설비/목공) 자동 추가 확인
```

### 3-2. 사전진단
```
□ /projects/[id]/diagnostic 접속
□ 체크리스트 항목 10개 이상 체크
□ 리스크 점수 실시간 계산되어 화면에 표시
□ GO/NO-GO 판정 표시
□ 저장 → risk_scores 테이블에 기록 확인
□ 진단 완료 후 공유 링크 자동 생성 확인
```

### 3-3. 견적서
```
□ /projects/[id]/sow 접속
□ 견적 항목 3개 이상 입력
□ 합계 금액 자동 계산
□ AI 분석 버튼 클릭 → /api/ai/quote-analyze 호출
□ AI 응답: 과다청구 항목 경고 표시
□ PDF 출력 버튼 → PDF 다운로드 확인
□ quote_line_items 테이블 저장 확인
```

### 3-4. 공정관리
```
□ /projects/[id]/process 접속
□ 공정 진행률 업데이트
□ 공정 완료 체크
□ 리스크 예측 자동 실행 확인
□ 전체 프로젝트 진행률 반영 확인
```

### 3-5. 현장 사진
```
□ /projects/[id]/gallery 접속
□ 사진 업로드 (Supabase evidence 버킷)
□ SHA-256 해시 자동 생성 확인
□ 날짜·시간 태그 확인
□ 업로드된 사진 목록 표시
```

### 3-6. 변경관리
```
□ /projects/[id]/changes 접속
□ 변경사항 입력 (제목, 내용, 비용 영향)
□ 저장 → change_orders 테이블 확인
□ 고객 알림 발송 확인
```

### 3-7. 3자 합의 + 전자서명
```
□ /projects/[id]/agreement 접속
□ 합의 내용 입력
□ 서명 패드에 서명
□ 서명 완료 → agreements 테이블 저장
□ SHA-256 해시 생성 확인
□ PDF 출력 확인
```

### 3-8. 하자 기록
```
□ /projects/[id]/defects 접속
□ 하자 사진 업로드
□ 심각도, 위치 입력
□ 저장 → defects 테이블 확인
□ 상태 변경 (접수→처리중→완료)
□ warranty_tracking 테이블에 담보기간 자동 계산 확인
```

### 3-9. 증빙 패키지
```
□ /projects/[id]/evidence-package 접속
□ 파일 업로드
□ Merkle Root 해시 생성 확인
□ /api/ai/check 호출 → AI 분석 응답
□ 패키지 PDF 출력 확인
```

### 3-10. AI 인증서
```
□ /projects/[id]/certificate 접속
□ 인증서 발급 버튼
□ 점수 계산 → 등급 표시 (A~F)
□ 인증서 코드 생성 (CHK-YYYY-XXXXX)
□ verification_certificates 테이블 저장
□ 공개 검증 링크 /verify/[code] 접속 확인
```

### 3-11. 공유 링크
```
□ 공유 버튼 클릭 → /api/share 호출
□ 공유 토큰 생성 → shares 테이블 저장
□ /share/[token] 접속 (로그인 없이)
□ 공사 현황, 사진, 견적, 공정, 인증서 표시 확인
□ 카카오톡 공유 버튼 동작 확인
□ 7일 만료 후 만료 화면 표시 확인
```

---

## 4단계 — 핵심 흐름 (고객)

### 4-1. 고객 초대
```
□ 업체가 프로젝트에서 고객 이메일 입력
□ 초대 이메일 발송 확인
□ 이메일 링크 클릭 → 가입 화면
□ 가입 완료 → /client/dashboard 이동
□ 해당 프로젝트만 접근 가능 확인
□ 다른 프로젝트 접근 시 차단 확인
```

### 4-2. 고객 홈
```
□ /client/dashboard 접속
□ 공사 진행률 % 표시
□ 오늘 현장 사진 표시
□ 확인 필요 항목 배지 표시
□ 최근 업데이트 목록 표시
```

### 4-3. 서명
```
□ 변경사항 알림 → 서명 화면
□ 서명 패드 동작 확인
□ 서명 완료 → 업체 알림 발송
□ 서명 후 해당 항목 완료 표시
```

### 4-4. 하자 접수
```
□ 하자 접수 버튼 → 사진 업로드
□ 위치·설명 입력
□ 접수 완료 → 업체 알림 발송
□ defects 테이블 저장 확인
```

---

## 5단계 — AI 기능

```
□ /ai-chat 에서 메시지 입력 → Gemini 응답
□ Gemini 실패 시 Claude 폴백 자동 전환
□ 분쟁 관련 키워드 입력 시 경고 배너 표시
□ 법규 관련 질문 시 RAG 검색 결과 포함
□ /api/ai/predict 호출 → 리스크 예측 결과
□ /api/ai/alerts → 알림 목록 반환
□ NotificationCenter 알림 표시
```

---

## 6단계 — 모바일 UX

```
□ Chrome DevTools 모바일 모드 (390px)에서 전체 화면 확인
□ 버튼 크기 최소 48px 확인
□ 하단 탭바 표시 확인
□ 사진 업로드 모바일에서 카메라 직접 실행 확인
□ 서명 패드 터치 동작 확인
□ 폼 입력 시 숫자 키패드 자동 활성화
□ 금액 입력 시 천 단위 콤마 자동
□ 스켈레톤 로딩 표시 확인
□ 토스트 메시지 표시 확인
```

---

## 7단계 — 오프라인

```
□ 개발자 도구 Network: Offline 설정
□ /offline 페이지 표시 확인
□ 오프라인 상태에서 기록 입력 가능 확인
□ 온라인 복구 후 자동 동기화 확인
```

---

## 8단계 — 결제

```
□ /pricing 접속 → 플랜 선택
□ Toss 결제 모달 실행
□ 테스트 카드로 결제 완료
□ 구독 활성화 확인
□ 결제 실패 시 오류 처리 확인
```

---

## 완성 판단 기준

**아래 두 흐름이 오류 없이 처음부터 끝까지 동작하면 완성이다.**

**업체 흐름:**
회원가입 → 프로젝트 생성 → 사전진단 → 견적서 → 공정 기록 → 공유 링크 생성 → 변경사항 서명 → 하자 기록 → 증빙 패키지 → AI 인증서

**고객 흐름:**
초대 이메일 → 가입 → 공사 현황 확인 → 변경사항 서명 → 하자 접수 → 증빙 다운로드
