# Check-In — 의사결정 기준

Claude Code가 작업 중 판단이 필요한 상황에서 이 기준을 따른다.
임의 판단 금지. 반드시 이 문서 기준으로 결정한다.

---

## 1. 우선순위 원칙

충돌이 생기면 아래 순서로 우선한다.

```
1순위 — 데이터 손실 방지
2순위 — 보안 (인증, RLS, 개인정보)
3순위 — 고객 화면 동작 (고객이 보는 것)
4순위 — 업체 화면 동작
5순위 — 성능
6순위 — 코드 품질
```

예시: 빠른 쿼리를 위해 RLS 비활성화 → 절대 안 됨. 2순위가 5순위보다 항상 앞선다.

---

## 2. 파일 충돌 시

파일 A 수정이 파일 B를 깨뜨린다면:

```
→ 두 파일 모두 고친다. 하나만 고치고 넘어가지 않는다.
→ 고칠 수 없으면 작업 중단하고 보고한다.
→ 임시방편으로 기존 기능 비활성화 금지.
```

---

## 3. 에러 처리 기준

**고객이 보는 화면 (share/[shareId], /client):**
```
→ 에러 메시지 절대 그대로 노출 금지
→ "잠시 후 다시 시도해주세요" 또는 "준비 중입니다"
→ console.error로 로깅은 유지
```

**업체가 보는 화면 (dashboard 이하):**
```
→ toast.error()로 간단한 메시지
→ 구체적 원인 한 줄 ("저장에 실패했습니다. 네트워크를 확인해주세요.")
→ 재시도 버튼 제공
```

**API route:**
```
→ 항상 { error: string } 형태로 반환
→ 500 에러 시 상세 내용 클라이언트에 노출 금지
→ 인증 실패 → 401, 권한 없음 → 403, 없는 리소스 → 404
```

---

## 4. TypeScript 타입 기준

```
→ any 타입 사용 절대 금지
→ unknown 사용 후 타입 가드 처리
→ 기존 src/types/ 파일 우선 사용
→ 새 타입 필요 시 src/types/에 추가, 인라인 정의 금지
→ 타입 오류 → any로 임시 처리 후 넘어가지 말 것. 반드시 해결 후 진행.
```

---

## 5. Supabase 쿼리 기준

```
→ RLS 비활성화 금지
→ service role key 클라이언트 코드에서 사용 금지
→ 대량 데이터 조회 시 .limit() 필수
→ 실시간 구독은 컴포넌트 unmount 시 반드시 unsubscribe
→ 에러 발생 시 throw, 무시 금지
```

---

## 6. AI 호출 기준

```
→ 직접 Gemini/Claude API 호출 금지
→ 반드시 src/lib/ai/brain.ts의 brain() 함수 사용
→ AI 응답 실패 시 → 사용자에게 "AI 분석을 잠시 후 다시 시도해주세요" 표시
→ AI 응답을 DB에 저장할 때 → 저장 실패해도 화면 결과는 보여줄 것
```

---

## 7. 고객 화면 전용 규칙

고객(/client, /share)은 비전문가다. 아래를 반드시 지킨다.

```
→ 전문 용어 금지: 리스크 스코어 / Merkle Tree / SHA-256 / upsert / RLS
→ 대신 쓸 말: 공사 진행률 / 기록 보호됨 / 위변조 불가 / 저장됨
→ 로딩 시간 3초 초과 → 반드시 진행 표시 (스피너 또는 스켈레톤)
→ 오류 발생 → "잠시 후 다시 시도해주세요" 단 한 줄만
→ 버튼은 반드시 48px 이상
→ 네이비 색상 사용 금지 (오렌지만)
```

---

## 8. 성능 기준

```
→ 페이지 초기 로딩 데이터 조회 → 3개 이하 쿼리로 제한
→ 이미지 업로드 → 5MB 이하 제한, 초과 시 안내
→ PDF 생성 → 서버에서 처리, 클라이언트 블로킹 금지
→ 목록 조회 → 기본 20개 limit, 더보기 방식
```

---

## 9. 막혔을 때 판단 기준

아래 순서로 처리한다.

```
1. ERRORS.md 확인 — 이미 알려진 문제인가?
2. COMPONENTS.md 확인 — 연결 관계 확인
3. 10분 안에 해결 안 되면 → 작업 중단하고 정확히 보고
   보고 형식: "파일명:줄번호에서 [문제 내용] 발생. [시도한 것]. 판단 필요."
4. 절대 하지 말 것: 문제를 숨기고 다음 작업으로 넘어가기
```

---

## 10. 절대 하지 말 것 (재확인)

```
→ 새 기능 추가 (지시 없는 것)
→ 기존 DB 테이블 컬럼 삭제
→ 환경변수 값 변경
→ 기존에 동작하던 기능 건드리기
→ 여러 기능 동시 수정
→ TODO 주석 남기고 미완성 코드 커밋
→ console.log 프로덕션 코드에 대량 남기기
→ any 타입으로 타입 오류 회피
→ RLS 비활성화로 쿼리 오류 회피
```
